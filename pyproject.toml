[tool.ruff]
target-version = "py314"
line-length = 120
exclude = [
    ".git",
    ".venv",
    "assets",
    "images",
]

[tool.ruff.lint]
# Keep CI focused on correctness issues while avoiding a large style-only refactor.
select = ["E9", "F63", "F7", "F82"]
